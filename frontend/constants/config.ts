// constants/config.ts

// ⚠️ Remplace ceci par l'IP locale de ton PC (visible via `ipconfig` sous Windows)
const BACKEND_IP = '172.20.10.3'; // ← à personnaliser selon ton réseau

export const API_URL = `http://${BACKEND_IP}:5000/api`;
